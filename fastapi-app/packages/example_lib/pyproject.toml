[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example-lib"
version = "0.1.0"
description = "Example uv workspace library — rename, replace, or delete."
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["example_lib"]
